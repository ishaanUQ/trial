# Dev and test tooling, used for local development and CI.
# Not shipped in either container image.
# Minimum versions are pinned for reproducibility while staying flexible on patch releases.
pytest>=8.2
pytest-cov>=5.0
hypothesis>=6.100
ruff>=0.5
mypy>=1.10
httpx>=0.27
